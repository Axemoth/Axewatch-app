# Robolectric runs on JDK 17 locally and in CI (see .github/workflows).
# SDK 36 sandboxes require JDK 21, so pin unit tests to 35. Per-class
# @Config(sdk = ...) overrides this when a newer sandbox is needed.
sdk=35
